# 🚀 部署上線完整步驟

## 第一步：準備 GitHub 倉庫

### 1. 初始化 Git（如果還沒有的話）

```bash
git init
git add .
git commit -m "Initial commit"
```

### 2. 在 GitHub 建立新倉庫

1. 前往 https://github.com/new
2. 建立新倉庫（例如：`drink-tracker`）
3. **不要**勾選「Initialize with README」

### 3. 推送到 GitHub

```bash
git remote add origin https://github.com/你的用戶名/drink-tracker.git
git branch -M main
git push -u origin main
```

---

## 第二步：部署後端（API 伺服器）

### 選項 A：Railway（推薦，最簡單）⭐

1. **前往 Railway**
   - 網址：https://railway.app/
   - 使用 GitHub 帳號登入

2. **建立新專案**
   - 點擊 "New Project"
   - 選擇 "Deploy from GitHub repo"
   - 選擇您的 `drink-tracker` 倉庫

3. **設定服務**
   - Railway 會自動偵測 Node.js
   - 如果沒有自動設定，手動設定：
     - **Root Directory**: 留空（根目錄）
     - **Start Command**: `node server/index.js`

4. **取得後端 URL**
   - 部署完成後，點擊服務
   - 在 "Settings" → "Domains" 可以看到 URL
   - 例如：`https://drink-tracker-production.up.railway.app`
   - **複製這個 URL，等一下會用到！**

---

### 選項 B：Render

1. **前往 Render**
   - 網址：https://render.com/
   - 使用 GitHub 帳號登入

2. **建立 Web Service**
   - 點擊 "New +" → "Web Service"
   - 連接您的 GitHub 倉庫

3. **設定**
   - **Name**: `drink-tracker-api`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `node server/index.js`
   - **Plan**: 選擇 Free

4. **取得後端 URL**
   - 部署完成後會顯示 URL
   - 例如：`https://drink-tracker-api.onrender.com`
   - **複製這個 URL！**

---

## 第三步：部署前端

### 使用 Vercel（推薦）⭐

1. **前往 Vercel**
   - 網址：https://vercel.com/
   - 使用 GitHub 帳號登入

2. **匯入專案**
   - 點擊 "Add New..." → "Project"
   - 選擇您的 `drink-tracker` 倉庫

3. **設定專案**
   - **Framework Preset**: Create React App
   - **Root Directory**: `client` ⚠️ 重要！
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`

4. **設定環境變數** ⚠️ 非常重要！
   - 點擊 "Environment Variables"
   - 新增：
     - **Name**: `REACT_APP_API_URL`
     - **Value**: 您的後端 URL（例如：`https://drink-tracker-production.up.railway.app`）
   - 點擊 "Save"

5. **部署**
   - 點擊 "Deploy"
   - 等待建置完成（約 1-2 分鐘）

6. **取得前端 URL**
   - 部署完成後會顯示 URL
   - 例如：`https://drink-tracker.vercel.app`
   - **這就是您的應用網址！** 🎉

---

### 使用 Netlify（替代方案）

1. **前往 Netlify**
   - 網址：https://www.netlify.com/
   - 使用 GitHub 帳號登入

2. **新增網站**
   - 點擊 "Add new site" → "Import an existing project"
   - 選擇您的 GitHub 倉庫

3. **設定建置**
   - **Base directory**: `client`
   - **Build command**: `npm run build`
   - **Publish directory**: `client/build`

4. **設定環境變數**
   - 點擊 "Site settings" → "Environment variables"
   - 新增：
     - **Key**: `REACT_APP_API_URL`
     - **Value**: 您的後端 URL

5. **部署**
   - 點擊 "Deploy site"

---

## 第四步：測試部署

### 測試後端 API

在瀏覽器開啟：
```
https://您的後端URL/api/health
```

應該會看到：
```json
{"status":"ok"}
```

### 測試前端應用

1. 開啟您的前端 URL（例如：`https://drink-tracker.vercel.app`）
2. 嘗試新增一筆記錄
3. 檢查是否能正常儲存和顯示

---

## 常見問題

### Q: 前端無法連接到後端？
**A:** 檢查：
1. 環境變數 `REACT_APP_API_URL` 是否正確設定
2. 後端 URL 是否包含 `https://`（不要包含尾部的 `/`）
3. 重新部署前端（環境變數變更需要重新建置）

### Q: Railway/Render 顯示部署失敗？
**A:** 檢查：
1. `package.json` 中的依賴是否正確
2. `server/index.js` 是否存在
3. 查看部署日誌中的錯誤訊息

### Q: 資料庫資料會保留嗎？
**A:** 
- **Railway**: 資料會保留在持久化儲存中
- **Render**: 免費方案會休眠，但資料會保留
- 建議定期備份資料庫檔案

---

## 快速檢查清單

- [ ] Git 倉庫已建立並推送到 GitHub
- [ ] 後端已部署（Railway 或 Render）
- [ ] 已取得後端 URL
- [ ] 前端已部署（Vercel 或 Netlify）
- [ ] 已設定 `REACT_APP_API_URL` 環境變數
- [ ] 後端健康檢查通過（`/api/health`）
- [ ] 前端可以正常新增和顯示記錄

---

## 完成！🎉

部署完成後，您就可以：
- ✅ 在任何裝置上使用應用
- ✅ 資料會自動同步
- ✅ 不需要保持電腦開機

**享受您的手搖飲料記錄應用吧！** 🥤
